# README screenshots

Drop the four files here with **exactly** these names — `README.md` already
references them:

| filename | what it should show | popup tab |
|---|---|---|
| `evidence-verdict.png` | the green "N/N regions verified pixel-uniform" bar with the sanitised frame below it | **Evidence** |
| `masks-proofs.png` | the per-region table: token, lens, what it was, box, proof | **Masks** |
| `selftest-assertions.png` | "all 8 assertions passed", the three-panel strip, the metrics row | (self-test page) |
| `settings-modes.png` | perception mode selector, WebGPU toggle, Lens B sensitivity | **Settings** |

Capture at a 2x display scale if you can, and crop to the browser viewport
(no OS menu bar, no dock).
